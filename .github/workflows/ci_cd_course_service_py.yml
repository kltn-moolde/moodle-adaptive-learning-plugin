name: CI/CD course-service

on:
  push:
    paths:
      - 'courseservice/**'
  pull_request:
    paths:
      - 'courseservice/**'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # Checkout code
      - uses: actions/checkout@v4

      - name: Log in to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # Build Docker image
      - name: Build Docker image
        run: |
          cd courseservice
          docker build -t ${{ secrets.DOCKER_USERNAME }}/course_service:latest .

      # Push image lên DockerHub
      - name: Push Docker image
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/course_service:latest

      # Optional: Deploy (tuỳ môi trường)
      - name: Deploy service
        uses: appleboy/ssh-action@v0.1.8
        with:
          host: ${{ secrets.SERVER_HOST }}
          port: ${{ secrets.SERVER_PORT }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            docker pull ${{ secrets.DOCKER_USERNAME }}/course_service:latest
            docker stop course_service || true
            docker rm course_service || true
            docker run -d \
                --name course_service \
                --network moodle-adaptive-learning-plugin_app-network \
                -p 5001:5001 \
                -e MONGO_URI="${{ secrets.MONGO_URI }}" \
                -e MOODLE_API_BASE="${{ secrets.MOODLE_API_BASE }}" \
                -e MOODLE_TOKEN="${{ secrets.MOODLE_TOKEN }}" \
                -e ADDRESS_MOODLE="${{ secrets.ADDRESS_MOODLE }}" \
                ${{ secrets.DOCKER_USERNAME }}/course_service:latest
            docker network connect moodle_project_default course_service